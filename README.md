# Анализ данных с использованием Kafka и ClickHouse

Система для поиска категории с максимальной транзакцией в каждом штате

## 🏗️ Архитектура

Компоненты системы:

1. **`kafka-producer`**:
    * Читает транзакции из `train.csv`
    * Отправляет каждую строку в топик Kafka `transactions`

2. **`clickhouse-query`**:
    * Выполняет SQL-запрос `query.sql` для поиска категории с самой большой транзакцией по каждому штату
    * Сохраняет результат в файл `submission.csv`

3. **`ClickHouse`**:
    * Потребляет данные из топика `transactions` с помощью Materialized View
    * Хранит данные в таблице `transactions` с применением оптимизаций

4. **`Kafka Infrastructure`**:
    * Zookeeper: Управляет и координирует кластер Kafka
    * Kafka Broker: Принимает, хранит и отдает потоковые данные

## Ключевые оптимизации

- **Ключ сортировки (`ORDER BY`)**
- **Партиционирование (`PARTITION BY`)**
- **Проекции (`PROJECTION`)**
- **Типы данных (`LowCardinality`)**

## 🚀 Быстрый старт

### Требования

- Docker 20.10+
- Docker Compose 2.0+

### Запуск

Последовательно выполните следующие команды:

**1. Запуск основных сервисов:**
```bash
docker-compose up -d --build
```
**2. Загрузка данных в Kafka:**
```bash
sleep 20
docker-compose run --rm kafka-producer
```
**3. Выполнение анализа:**
```bash
sleep 20
docker-compose run --rm clickhouse-query
```

Результат сохраняется в файл `submission.csv`

## Структура проекта

```
.
├── clickhouse_init.py     # Выполнение запроса к ClickHouse
├── ddl.sql                # Создание таблиц
├── docker-compose.yml     # Оркестрация сервисов
├── Dockerfile             # Сборка сервисов
├── load_kafka.py          # Загрузка данных в Kafka
├── query.sql              # Аналитический запрос
├── README.md              # Инструкиця к использованию
├── requirements.txt       # Необходимые баблиотеки
└── train.csv              # Исходные данные
```
